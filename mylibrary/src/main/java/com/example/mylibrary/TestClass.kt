package com.example.mylibrary

import android.content.Context
import androidx.appcompat.app.ActionBar
import androidx.lifecycle.LiveData

class TestClass {
    fun test(context: Context, bar: ActionBar, liveData: LiveData<*>) {
    }
}